import React from "react"

const WhatAreDifferences = () => (
  <>
    <h2>
      So, what <em>are</em> the differences?
    </h2>
    <p>
      Let me preface this with clarifying that we're talking about type aliases,
      and not a type literal like <strong>any</strong> for example.
    </p>
    <p>
      You can think of an interface as a virtual structure in which it defines
      the properties and keys of an object, and can also contain methods and
      nested objects within it as well. Types are similar in this regard, minus
      a few subtle differences.
    </p>
    <p>Let's write some code and break them down:</p>
  </>
)

export default WhatAreDifferences
